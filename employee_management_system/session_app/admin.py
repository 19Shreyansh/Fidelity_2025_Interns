# session = logger file. stored in database, file or cache but by default in database
# http has 2 objects . Request and Response

from django.contrib import admin

# Register your models here.
